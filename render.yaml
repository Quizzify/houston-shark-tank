services:
  - type: web
    name: houston-shark-tank
    runtime: static
    buildCommand: bash build.sh
    envVars:
      - key: SPOTS_REMAINING
        value: "5"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /shark-tank-landing.html
